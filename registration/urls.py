from django.urls import path
from . import views

urlpatterns = [
    # Other URL patterns
    path('success/', views.success_view, name='success'),
]
from django.urls import path
from . import views

urlpatterns = [
    # Other URL patterns
    path('success/', views.success_view, name='success'),
]
